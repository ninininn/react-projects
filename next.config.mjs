const nextConfig = {
    output: 'export',
    // distDir: './dist',
    basePath: "/react-projects",
    images: { unoptimized: true },
    assetPrefix: "/react-projects",
};

export default nextConfig;